server:
  address: 0.0.0.0

spring:
  cloud:
    azure:
      keyvault:
        secret:
          property-source-enabled: false
      credential:
        managed-identity:
          enabled: false
  datasource:
    username: db_a986f5_palmflow_admin
    password: UumbalD3v3l0pm3nt#
    url: jdbc:sqlserver://SQL8012.site4now.net;databaseName=db_a986f5_palmflow;trustServerCertificate=true
  jpa:
    hibernate:
      ddl-auto: validate
  thymeleaf:
    cache: false
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:maguillengo@gmail.com}
    password: ${MAIL_PASSWORD:jfew honu xnju vabz}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  cors:
    allowed-origins: http://localhost:4200,http://localhost:8100,http://localhost:5173,http://127.0.0.1:4200,http://127.0.0.1:8100,http://localhost,https://localhost,capacitor://localhost,ionic://localhost
  mail:
    from: maguillengo@gmail.com
  frontend:
    base-url: http://localhost:4200

palm-flow:
  seed:
    geografia:
      enabled: false
      excel-path: docs/helper-files/Trazabilidad_2026.xlsx
    centros-acopio:
      enabled: false
      excel-path: docs/helper-files/Acopios Uumbal_junio2026.xlsx
      sheet-name: Acopios
    productores-predios:
      enabled: false
      excel-path: docs/helper-files/Trazabilidad_2026.xlsx
      clear-existing: false
      batch-size: 150