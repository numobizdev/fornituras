export const STORAGE_KEYS = {
  token: 'sigefor.auth.token',
  tokenType: 'sigefor.auth.tokenType',
  expiresAt: 'sigefor.auth.expiresAt',
  user: 'sigefor.auth.user',
  tourHomeDone: 'sigefor.tour.home.done',
} as const;
